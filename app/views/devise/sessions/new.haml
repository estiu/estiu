-# It's important that in the *login* page, password option appears first. We don't want users to potentially create a second account.
.row.devise-user-form
  .bordered{class: centered}
    %h3= t '.title'
    %hr
    %h4= t('.password_case') + ':'
    = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      = form_fields(f, 'devise.sessions.new') do |field|
        = field.(:email, autofocus: true)
        = field.(:password)
        = field.(:submit)
    = render "devise/shared/links"
    %hr.top0
    %h4= t('.fb_case') + ':'
    = facebook_connect :login
    .bottom20